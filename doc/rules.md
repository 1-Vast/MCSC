# DrugTarget Rules

The active model is MCSC-FrozenAlpha. Keep the repository focused on:

- current trainable MCSC mainline;
- reproduced deep baselines;
- SOTA evidence builder;
- leakage/GPU/compile gates;
- experiment folders that record comparisons and analysis.

Do not reintroduce retired selector, dispersion, RCSC, RA-MCSC, or broad
architecture-search paths unless a new experiment is explicitly requested.

Do not claim global SOTA. The allowed claim is reproduced-frontier SOTA-level
under this repository's same split/seed/metric protocol.

LLM/DeepSeek text is optional audit material only and is not an active MCSC
target representation.
